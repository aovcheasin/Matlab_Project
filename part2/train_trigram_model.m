function trigramModel = train_trigram_model(corpus)
%TRAIN_TRIGRAM_MODEL Train a trigram model from a tokenized corpus
%   trigramModel = train_trigram_model(corpus)
%   Optional/extra-credit: similar shape to bigram model but for trigrams

trigramModel = struct('counts',[], 'probs',[]);
% TODO: implement trigram counts and smoothing

end
