function report = compare_predictions(testSet, models)
%COMPARE_PREDICTIONS Compare outputs of n-gram and vector models
%   report = compare_predictions(testSet, models)
%   testSet: tokenized test sentences
%   models: struct with fields e.g. bigramModel, trigramModel, embeddings

report = struct('accuracy',[],'details',[]);
% TODO: implement evaluation and comparison logic

end
